# ---- API KEYS ----
adspower_api_key: ""     # лишаємо пустим, якщо не використовуємо Cloud API

# ---- PROXIES (на майбутнє) ----
proxies:
  enabled: false
  list:
    - host: "127.0.0.1"
      port: 8000
      user: ""
      password: ""