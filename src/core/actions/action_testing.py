"""Тимчасовий екшен для майбутніх експериментів з тестуванням."""

from __future__ import annotations

from typing import Any, Sequence, TYPE_CHECKING

if TYPE_CHECKING:  # pragma: no cover - використовується лише для підказок типів.
    from src.core.ads_power import AdsPower


def action_testing(ads: "AdsPower", payload: Sequence[Any]) -> bool:
    """Приймає клієнт AdsPower та масив даних для експериментальних викликів.

    Поки що екшен виконує лише логування отриманих даних, але залишає
    підготовлену точку входу для подальших досліджень. Очікується, що в майбутньому
    тут будуть розміщені виклики до допоміжних функцій, які потребують доступу до
    ``AdsPower`` та різноманітних вхідних даних.
    """

    # Фіксуємо, що екшен стартував та отримав усі необхідні аргументи.
    print("[ACTION TESTING] 🧪 Запускаю тестовий сценарій з переданими даними.")
    print(
        "[ACTION TESTING] 📦 Отримано масив з",
        len(payload),
        "елемент(ами) для експерименту.",
    )

    # Тут у майбутньому можна буде звертатися до ``ads`` для виклику API AdsPower.
    # Коментар допомагає не забути, що клієнт передається першим параметром.
    _ = ads

    # Повертаємо ``True`` як підтвердження успішного виконання тестової процедури.
    return True


__all__ = ["action_testing"]
