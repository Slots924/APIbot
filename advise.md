📄 advise.md
⚠️ Проблеми, з якими ми зіткнулися при автоматизації Facebook (лайки + коментарі)
✅ 1. Повільне завантаження сторінки через проксі AdsPower

Симптоми:

DOM частково завантажений

елементи не знаходяться або стають stale

скрипт вважав, що лайк вже стоїть (хоча сторінка ще не прогрузилась)

Рішення:

використовувати DOM stabilization – чекати поки HTML перестане змінюватись (page_source).

уникати driver.get() → замість цього переходити через
driver.execute_script("window.location.href = ...")

додати невеликі рандомні паузи

👍 2. Кнопка Like була невидима або перекрита інтерфейсом

Симптоми:

ElementClickInterceptedException

JS-клік не спрацьовував

лайк не ставився, хоча лог казав “Поставлено”

Причини:

кнопка Like фізично нижче viewport

FB підвантажує блоки при скролі

React перерендерює компонент

Рішення:

мінімальний скрол вниз (≈400–600px) лише один раз перед пошуком кнопки

fallback: якщо не вдалось клікнути → JS click

повторне підтвердження лайку після кліку (перевірити стан)

💬 3. Поле для коментаря “стигало” та зникало під час набору

Симптоми:

курсор зникав при друці

символи не вводилися або вводилися не туди

StaleElementReferenceException

після кліку React перевідмальовував contenteditable → фокус пропадав

Причина:

Facebook НЕ використовує стандартну <textarea>
Він використовує React div[contenteditable="true"], який пересоздається під час взаємодії.

Рішення:

не скролити перед взаємодією (щоб не тригерити догрузку елементів)

клікати в поле двічі з паузою

перед набором і під час набору періодично повторно фокусувати contenteditable

друк через Chrome DevTools Protocol (Input.insertText) замість send_keys

стабільно працює навіть після перерендеру

виглядає як справжній користувацький ввід

чекати DOM stabilization перед взаємодією

👤 4. Втрата фокусу при друці коментаря

Причина:

Selenium send_keys генерує key events, які FB іноді обробляє як “ненатуральні”, що призводить до blur та ре-рендера поля.

Рішення:

використовувати Input.insertText через CDP

періодично уточнювати фокус і каретку перед вставленням символів

невеликі рандомні затримки між символами (імітація людини)

🧠 5. FB анти-бот механіки

FB моніторить:

різкі кліки без прокрутки

дуже швидкий набір тексту

однотипні патерни поведінки

Методи уникнення:

додано human_behavior() (імітація скролу/мишки/пауза)

рандомні затримки при вводі тексту

перевірка станів перед дією

🏁 Підсумки
Проблема	Причина	Рішення
Лайк не ставився / пропускався	DOM не завантажений / кнопка перекрита	DOM стабілізація, скрол 1 раз, JS fallback
Курсор зникав під час друку	React перерендерює поле	CDP InsertText + рефокус
StaleElementException	елемент оновився після взаємодії	повторний пошук + рефокус
FB фіксує бота	надто “ідеальна” швидкість	рандомні затримки, human-behavior